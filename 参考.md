跨器官分期对齐方案：从“极端对齐”到“轴向流形”

本文档总结了两种用于解决前列腺（Source）与直肠（Target）跨器官 MRI 分期任务的对齐方案。

方法一：极端分期的「软锚定对齐」（Soft Anchor Alignment, SAA）

这是最稳健、最容易被接受、改动最小的方案。

1. 方法动机（Method Motivation）

在跨器官分期任务中，绝对分期标签不可直接对齐，但疾病进展的“语义极端”是可比较的：

最低分期（Healthy / No Tumor）：无肿瘤浸润的组织状态。

最高分期（Severe Invasion）：明确的结构破坏与组织外侵。

由于器官解剖差异与 MRI 表观分布差异，直接将样本或特征进行强制对齐会破坏中间分期的序数流形结构。因此，SAA 不对齐样本、不对齐单点，仅对“类别中心（Prototype）”施加软约束，以提供跨器官的弱先验。

2. 数学建模（Formulation）

(1) 定义跨域极端类别原型

对于源域（src）与目标域（tgt），分别计算：

最低分期原型：$\mu^{src}_{0}, \mu^{tgt}_{0}$

最高分期原型：$\mu^{src}_{max}, \mu^{tgt}_{max}$

其中，原型是通过特征均值计算得到的（支持 Batch-level 或 EMA 更新）：


$$\mu^{d}_{k} = \frac{1}{|C^{d}_{k}|} \sum_{i \in C^{d}_{k}} f_i$$

(2) 软锚定损失定义

低分期（健康）— 分布一致性约束：


$$L_{anchor}^{low} = \|\mu^{src}_{0} - \mu^{tgt}_{0}\|_2^2$$


含义：健康组织在“无浸润语义”上应接近，但允许解剖差异。

高分期（重度）— 方向一致性约束：


$$L_{anchor}^{high} = 1 - \cos(\mu^{src}_{max}, \mu^{tgt}_{max})$$


含义：强调“浸润方向”的一致性，而非强制特征幅值或形态一致。

(3) 总软锚定损失

$$L_{SAA} = \lambda_{low} L_{anchor}^{low} + \lambda_{high} L_{anchor}^{high}$$

3. 训练与实现逻辑

接入阶段：仅在 Phase 2（Joint Training）中启用。

梯度流向：只作用于 Backbone embedding。

权重建议：$\lambda_{low} \approx 0.05$，$\lambda_{high} \approx 0.1$。

方法二：共享「疾病进展轴」的序数对齐（Shared Progression Axis, SPA）

这是最符合“序列学习本质”直觉、理论深度最高的方案。

1. 方法动机（Method Motivation）

虽然前列腺癌 PI-RADS 与直肠癌 T 分期的离散定义不同，但二者本质上都描述了从“无浸润”到“局部浸润”再到“结构破坏”的连续疾病进程。与其对齐特征空间，不如对齐“疾病进展方向”本身。

2. 数学建模（Formulation）

(1) 进展轴定义

在 Backbone 输出的 Embedding $f \in \mathbb{R}^D$ 上，引入一个共享的一维投影向量 $v \in \mathbb{R}^D$：


$$z = v^\top f \quad \text{其中 } v \in \mathbb{R}^D$$

$v$ 在两个器官间完全共享。

$z$ 表示投影到该轴上的“疾病进展程度”。

(2) 极端分期的轴向约束

定义归一化目标 $\tilde{y}$：


$$\tilde{y} = \begin{cases} 0, & \text{lowest stage} \\ 1, & \text{highest stage} \end{cases}$$


轴向损失：


$$L_{axis} = \sum_{i \in \{low, high\}} (z_i - \tilde{y}_i)^2$$

(3) 与回归任务的融合

已有回归头 $\hat{y} = g(f)$，现在可以：

让 $z$ 作为辅助回归监督。

或将 $z$ 作为回归 Head 的一维先验特征。

3. 训练与实现逻辑

训练方式：SPA 不替代原有 Loss，作为一个弱序数正则项。

总损失更新：


$$L_{Total} \leftarrow L_{Total} + \lambda_{axis} L_{axis}$$

权重建议：$\lambda_{axis} \approx 0.05$。
