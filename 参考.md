# 跨器官分期对齐方案：从“极端对齐”到“轴向流形”

本文档总结了两种用于解决**前列腺（Source）**与**直肠（Target）**跨器官 MRI 分期任务的对齐方案，分别从**弱先验锚定**与**序数流形一致性**两个角度出发，避免直接进行高风险的特征强对齐。

---

## 方法一：极端分期的「软锚定对齐」
**Soft Anchor Alignment（SAA）**

这是最稳健、最容易被接受、改动最小的方案。

---

### 1. 方法动机（Method Motivation）

在跨器官分期任务中，**绝对分期标签不可直接对齐**，但疾病进展的**语义极端状态**在不同器官之间具有可比性：

- **最低分期（Healthy / No Tumor）**  
  表示无肿瘤浸润的组织状态。
- **最高分期（Severe Invasion）**  
  表示明确的结构破坏与组织外侵。

然而，由于**器官解剖结构差异**与 **MRI 表观分布差异**，若直接对样本或特征进行强制对齐，将破坏中间分期所应满足的**序数流形连续性**。

因此，SAA 采用如下策略：

> **不对齐样本、不对齐单点，仅对“类别中心（Prototype）”施加软约束**，  
> 以引入跨器官的弱先验，而不干扰局部判别结构。

---

### 2. 数学建模（Formulation）

#### (1) 跨域极端类别原型定义

对于源域（src）与目标域（tgt），分别定义：

- 最低分期原型：  
  \[
  \mu^{src}_{0}, \quad \mu^{tgt}_{0}
  \]

- 最高分期原型：  
  \[
  \mu^{src}_{max}, \quad \mu^{tgt}_{max}
  \]

其中，原型通过特征均值计算得到（支持 Batch-level 或 EMA 更新）：

\[
\mu^{d}_{k} = \frac{1}{|C^{d}_{k}|} \sum_{i \in C^{d}_{k}} f_i
\]

---

#### (2) 软锚定损失定义

**低分期（健康）—— 分布一致性约束：**

\[
L_{anchor}^{low} = \| \mu^{src}_{0} - \mu^{tgt}_{0} \|_2^2
\]

含义：  
健康组织在“无浸润语义”上应保持接近，但允许器官特异的结构差异。

---

**高分期（重度）—— 方向一致性约束：**

\[
L_{anchor}^{high} = 1 - \cos(\mu^{src}_{max}, \mu^{tgt}_{max})
\]

含义：  
强调“侵润方向”的一致性，而非强制特征幅值或形态完全一致。

---

#### (3) 总软锚定损失

\[
L_{SAA} = \lambda_{low} L_{anchor}^{low} + \lambda_{high} L_{anchor}^{high}
\]

---

### 3. 训练与实现逻辑（Implementation）

- **接入阶段**：仅在 Phase 2（Joint Training）中启用  
- **梯度流向**：仅作用于 Backbone embedding  
- **权重建议**：  
  \[
  \lambda_{low} \approx 0.05, \quad \lambda_{high} \approx 0.1
  \]

---

## 方法二：共享「疾病进展轴」的序数对齐  
**Shared Progression Axis（SPA）**

这是最符合“序列学习本质”直觉、理论深度最高的方案。

---

### 1. 方法动机（Method Motivation）

尽管前列腺癌 PI-RADS 分期与直肠癌 T 分期在**离散定义上并不一致**，但二者本质上均刻画了同一连续过程：

> 从 **无浸润** → **局部浸润** → **结构破坏**

因此，与其强行对齐高维特征空间，不如仅对齐**疾病进展的单调方向本身**。

---

### 2. 数学建模（Formulation）

#### (1) 疾病进展轴定义

在 Backbone 输出的嵌入特征  
\[
f \in \mathbb{R}^D
\]
之上，引入一个**共享的一维投影向量**：

\[
z = v^\top f, \quad v \in \mathbb{R}^D
\]

其中：

- 向量 \( v \) 在前列腺与直肠任务间**完全共享**
- 标量 \( z \) 表示样本在“疾病进展轴”上的位置

---

#### (2) 极端分期的轴向约束

定义归一化目标：

\[
\tilde{y} =
\begin{cases}
0, & \text{lowest stage} \\
1, & \text{highest stage}
\end{cases}
\]

轴向监督损失为：

\[
L_{axis} = \sum_{i \in \{low,\, high\}} (z_i - \tilde{y}_i)^2
\]

该约束仅作用于**语义极端样本**，不干扰中间分期的相对关系。

---

#### (3) 与回归任务的融合方式

设已有回归头为：

\[
\hat{y} = g(f)
\]

则 SPA 可通过两种方式融入现有体系：

- 将 \( z \) 作为**辅助回归监督信号**
- 将 \( z \) 作为回归 Head 的**一维先验特征**

---

### 3. 训练与实现逻辑（Implementation）

- **训练策略**：  
  SPA 不替代任何已有损失，仅作为弱序数正则项引入
- **总损失更新**：
  \[
  L_{Total} \leftarrow L_{Total} + \lambda_{axis} L_{axis}
  \]
- **权重建议**：
  \[
  \lambda_{axis} \approx 0.05
  \]

---

## 小结

- **SAA** 提供跨器官的**弱全局锚点先验**，稳健、易实现  
- **SPA** 对齐的是**疾病进展方向而非特征空间**，理论一致性最强  
- 二者均避免了高风险的强特征对齐，适用于小样本、强序数约束的医学分期任务
