# **Cross-Organ Cancer Progression Analysis via Bi-Head Synergistic Learning**

### **基于双头协同学习与生物学行为对齐的直肠癌 T 分期预测模型**

## **1\. 任务背景 (Clinical Context)**

直肠癌 T 分期（T1-T4）的精准预测对临床决策至关重要，尤其是区分 **T2（局限于肌层）** 与 **T3（突破肌层）** 这一手术红线。本项目通过引入大规模强标注的前列腺癌（ISUP 0-5）作为源域知识库，解决直肠癌样本稀缺下的特征表征难题。

## **2\. 核心架构 (Architecture Overview)**

模型采用 **“共享骨干网 (EfficientNet-V2S) \+ 双任务投影头”** 的非对称架构：

* **特征头 (Feature Head,** $f\_i$**)**: 构建高维解剖流形空间，负责提取跨器官共享的肿瘤演进特征。  
* **回归头 (Regression Head,** $\\hat{\\tilde{y}}\_i$**)**: 执行最终的恶性进度条映射，输出 $\[0, 1\]$ 之间的连续分值。

## **3\. 损失函数深度建模 (Loss Function Modeling)**

我们将监督逻辑划分为四个功能模块，每个模块均有明确的临床动机与数学目标。

### **模块 I：全局轨迹对齐损失 ($L\_{Align}$)**

**【目的】** 消除前列腺与直肠间的解剖分布差异（Domain Shift），建立统一的“恶性演进坐标系”。

$$L\_{Align} \= \\underbrace{\\lambda\_{a} \\left\[ \\|\\mu\_{0}^s \- \\mu\_{0}^t\\|^2 \+ (1 \- \\cos(\\bar{\\mu}\_{high}^s, \\bar{\\mu}\_{high}^t)) \\right\]}\_{\\text{特征层 (Feature-level)}} \+ \\underbrace{\\gamma \\frac{1}{N} \\sum\_{i=1}^N \\| \\text{Sigmoid}(v^T f\_i) \- \\tilde{y}\_i \\|^2}\_{\\text{输出层 (Output-level)}}$$

* **功能逻辑**：  
  * **起点重合**：$\\mu\_{0}^s, \\mu\_{0}^t$ 代表前列腺与直肠的健康组织特征中心。强制起点重合可确立演进轨迹的“生物学零点”。  
  * **终点同向**：$\\bar{\\mu}\_{high}$ 代表晚期癌特征。余弦相似度约束确保两者虽形态不同，但恶性进展的方向语义保持平行。  
  * **轴线投影**：通过共享权重 $v$ 将特征强制投影至一维进展轴。

### **模块 II：解剖感知序数对比 ($L\_{Ordinal\\\_A}$)**

**【目的】** 强化对关键临床决策点（T2 vs T3）的感知力，精准捕捉边界穿透特征。

$$L\_{Ordinal\\\_A} \= \\underbrace{-\\sum\_{i \\in B} w\_i \\log \\frac{\\exp(f\_i^T c\_{y\_i} / \\tau)}{\\sum\_{j} \\exp((f\_i^T c\_j \+ \\rho |\\tilde{y}\_i \- \\tilde{y}\_j|) / \\tau)}}\_{\\text{序数中心对比 (Ordinal Prototyping)}} \+ \\underbrace{\\alpha(\\tilde{y}\_i) (1 \- \\cos(f\_i, P\_{trans}))}\_{\\text{穿透行为感知 (Penetration Aware)}}$$

* **功能逻辑**：  
  * **等级聚类**：强制样本向所属分期的原型 $c\_{y\_i}$ 聚类，$\\rho |\\tilde{y}\_i \- \\tilde{y}\_j|$ 保证了特征空间的序数严谨性。  
  * **穿透报警**：针对高危样本（$y \\ge 3$），激活“穿透原型” $P\_{trans}$ 约束，强制模型学习突破肌层或包膜的特异性视觉信号。

### **模块 III：流形平滑回归 ($L\_{Manifold}$)**

**【目的】** 保证疾病演进在特征流形上的局部拓扑连续性，避免临床诊断分值的非正常跳变。

$$L\_{Manifold} \= \\underbrace{\\frac{1}{N} \\sum\_{i=1}^N (\\tilde{y}\_i \- \\hat{\\tilde{y}}\_i)^2}\_{\\text{诊断准确性 (MSE)}} \+ \\underbrace{\\beta \\sum\_{i,j} \\exp(-|\\tilde{y}\_i \- \\tilde{y}\_j|) \\cdot \\|f\_i \- f\_j\\|^2}\_{\\text{空间平滑性 (Topological Smoothing)}}$$

* **功能逻辑**：  
  * **拓扑保持**：利用图拉普拉斯正则项，强制临床分期接近的样本在特征空间中保持邻近。  
  * **闭环回归**：作为回归头的核心判分项，确保最终预测值精准贴合临床分期标签。

### **模块 IV：一致性正则化 ($L\_{Consist}$)**

**【目的】** 利用模糊/噪声数据进行抗噪训练，提升模型在复杂低质量影像下的诊断鲁棒性。

$$L\_{Consist} \= \\frac{1}{N} \\sum\_{i \\in D\_{Target\\\_N}} \\| \\hat{\\tilde{y}}\_i^{weak} \- \\hat{\\tilde{y}}\_i^{strong} \\|^2$$

* **功能逻辑**：  
  * **稳定表征**：针对 50 例模糊样本，通过强弱增强的一致性约束，引导回归头“穿透噪声”锁定肿瘤核心演进状态。

## **4\. 训练分发与验证流程 (Pipeline & Dispatch)**

### **4.1 数据流动态路由 (Dynamic Routing)**

在训练 Batch 中，系统根据数据身份（Domain ID）自动激活损失分支：

* **前列腺 (1500) & 干净直肠 (200)**: $\\rightarrow$ 全激活 ($L\_{Align} \+ L\_{Ordinal\\\_A} \+ L\_{Manifold}$)  
* **模糊直肠 (50)**: $\\rightarrow$ 仅激活回归路径及 $L\_{Consist}$

### **4.2 临床推理模式 (Inference Mode)**

验证阶段完全剥离辅助的特征头（Feature Head）及其损失函数：

1. **输入**: 独立的直肠癌测试集。  
2. **路径**: Backbone $\\rightarrow$ Regression Head。  
3. **输出**: 预测标量 $\\hat{\\tilde{y}}$。  
4. **评判**: 映射回 T-stage 离散等级，重点评估 T2/T3 的区分灵敏度。

## **5\. 结论：为什么本方案能冲击 MICCAI？**

1. **生物共性定义**: 首次通过 $P\_{trans}$ 共享原型定义了跨器官的肿瘤“穿透”语义。  
2. **解剖结构解耦**: 训练时多维度强监督（练内功），推理时极简回归（出快招），实现精度与效率的最佳平衡。  
3. **异构知识对齐**: 巧妙将 1500 例前列腺大数据作为“导航仪”，显著提升了 200 例直肠癌小样本的 T 分期精度。
