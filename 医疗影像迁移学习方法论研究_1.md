# 跨器官肿瘤进展分析模型架构方案

## 第一部分：模型架构深度解构

本架构采用 **“共享骨干网 + 异构多头并行”** 的非对称设计。为了克服跨器官（前列腺至直肠）的特征分布差异，引入了 **SPA 轴向投影头**，利用强标注的源域提取通用的肿瘤浸润语义，并强制源域与目标域在潜空间实现端对端对齐。



### 1. 共享骨干网 (Shared Backbone, $E$)
* **选型**：`EfficientNet-V2S`。
* **逻辑**：作为通用的“解剖特征编码器”。无论前列腺还是直肠，其底层的视觉特征在生物学上是高度一致的。
* **输出**：特征嵌入向量 $f \in \mathbb{R}^D$。

### 2. 并行任务投影头 (Parallel Task-specific Heads)
在编码器输出 $f$ 之后，并行挂载以下六个模块：
* **[NEW] 共享进展轴头 (SPA Head)**：由一个共享权重向量 $v$ 和 Sigmoid 激活组成，将高维特征 $f$ 压缩为一维的 $[0,1]$ 标量。
* **原型投影头 (PCOL Head)**：在潜空间建立全局的类别中心（Prototypes）。
* **序数对比头 (SCOLw Head)**：在样本之间建立严谨的序数距离约束。
* **边界感知头 (BTA Head)**：专门提取图像高频特征，判断“穿透”状态。
* **拓扑保持头 (Topo Head)**：通过图拉普拉斯正则化维持疾病进展的连续流形。
* **回归预测头 (Regression Head)**：最终输出 $\hat{y}$ 的连续预测值。

---

## 第二部分：全口径损失函数数学建模

### 一、 跨域对齐与全局约束（新增核心）

#### 1. 跨器官软锚定对齐损失 ($L_{SAA}$)
$$L_{SAA} = \lambda_{low} \underbrace{\|\mu_0^s - \mu_0^t\|_2^2}_{\text{起点重合}} + \lambda_{high} \underbrace{\left( 1 - \frac{\mu_{max}^s \cdot \mu_{max}^t}{\|\mu_{max}^s\| \|\mu_{max}^t\|} \right)}_{\text{终点同向}}$$

#### 2. 共享疾病进展轴损失 ($L_{SPA}$)
$$z_i = \text{Sigmoid}(v^T f_i)$$
$$L_{SPA} = \frac{1}{N} \sum_{i=1}^N \|z_i - \tilde{y}_i\|^2$$

### 二、 序数回归与对比学习（原有核心）

#### 3. 改进的加权序数对比损失 ($L_{SCOLw}$)
$$L_{SCOLw}=\sum_{i \in B}-w_i\frac{1}{|P(i)|}\sum_{p \in P(i)}\log\frac{\exp(f_i^T \cdot f_p / \tau)}{\sum_{n \in N(i)}\exp((f_i^T \cdot f_n + r_{i,n}) / \tau)}$$

#### 4. 原型对比序数损失 ($L_{PCOL}$)
$$L_{PCOL}=-\sum_{i \in \mathcal{I}}\log\frac{\exp(f_a^T \cdot c_p / \tau)}{\sum_{c_n \in N(i)}\exp((f_a^T \cdot c_n + r_{a,n}) / \tau)}$$

### 三、 解剖特征与鲁棒性（辅助任务）

#### 5. 边界穿透感知损失 ($L_{BTA}$)
仅当样本标签 $y \ge 3$ 时激活：
$$L_{BTA}=1-\cos(f_i, P_{trans})$$

#### 6. 拓扑保持流形损失 ($L_{Topo}$)
$$L_{Topo}=\sum_{i,j}W_{ij}\|f_i - f_j\|^2$$

#### 7. 连续回归预测损失 ($L_{RMSE}$)
$$L_{RMSE}=\sqrt{\frac{1}{N}\sum_{i=1}^N(y_i - \hat{y}_i)^2}$$

#### 8. 一致性正则化损失 ($L_{Consist}$)
$$L_{Consist}=D_{KL}(\sigma(E(Aug_{weak}(x_u))) \| \sigma(E(Aug_{strong}(x_u))))$$

---

## 第三部分：训练流程与策略

### 1. 阶段一：骨干网热身与轴线建立
* **输入**：全量前列腺癌数据 ($D_{Src}$)。
* **核心 Loss**：$L_{Phase1} = L_{RMSE} + L_{SPA} + L_{SCOLw}$。
* **目的**：确立通用“病情恶化”标尺。

### 2. 阶段二：多任务联合优化与跨域对齐
* **策略**：混合数据，利用 $L_{SAA}$ 强制拉齐两域起止点。
* **分流路径**：
    * **干净样本**：应用全量监督 Loss + 对齐 Loss。
    * **模糊样本**：通过 $L_{Consist}$ 进行一致性正则，避免脏标签干扰。

### 3. 阶段三：精细化微调
* **操作**：锁定 Backbone 和 SPA 标尺，仅微调回归头。
* **目的**：在对齐后的特征空间内进行最后的精度收敛。
