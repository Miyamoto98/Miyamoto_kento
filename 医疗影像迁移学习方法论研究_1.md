# 跨器官肿瘤进展分析模型架构方案

## 第一部分：模型架构深度解构

本架构采用 **“共享骨干网 + 异构多头并行”** 的非对称设计。为了克服跨器官（前列腺至直肠）的特征分布差异，引入了 **SPA 轴向投影头**，利用强标注的源域提取通用的肿瘤浸润语义，并强制源域与目标域在潜空间实现端对端对齐。

### 1. 共享骨干网 (Shared Backbone, $E$)
* **选型**：`EfficientNet-V2S`。
* **逻辑**：作为通用的“解剖特征编码器”。无论前列腺还是直肠，其底层的视觉特征（如 T2 加权下的低信号线断裂、边缘毛刺、组织纹理异质性）在生物学上是高度一致的。
* **输出**：特征嵌入向量 $f \in \mathbb{R}^D$。

### 2. 并行任务投影头 (Parallel Task-specific Heads)
在编码器输出 $f$ 之后，并行挂载以下 **六个** 模块：

* **共享进展轴头 (SPA Head)**：
    * **结构**：由一个共享权重向量 $v$ 和 Sigmoid 激活组成。
    * **作用**：将高维特征 $f$ 压缩为一维的 $[0,1]$ 标量，代表“疾病严重度”的绝对刻度，用于跨器官的全局对齐。
* **原型投影头 (PCOL Head)**：在潜空间建立全局的类别中心（Prototypes）。
* **序数对比头 (SCOLw Head)**：在样本之间建立严谨的序数距离约束。
* **边界感知头 (BTA Head)**：专门提取图像高频特征，判断“穿透”状态。
* **拓扑保持头 (Topo Head)**：通过图拉普拉斯正则化维持疾病进展的连续流形。
* **回归预测头 (Regression Head)**：最终输出 $\hat{y}$ 的连续预测值，系推理阶段的主输出。

---

## 第二部分：全口径损失函数数学建模

本方案集成了 8 个损失函数，分为“特征对齐”、“序数约束”、“解剖感知”三大类。

### 一、 跨域对齐与全局约束（新增核心）

#### 1. 跨器官软锚定对齐损失 ($L_{SAA}$)
**目的**：在特征空间强行拉齐源域（前列腺 $s$）和目标域（直肠 $t$）的起止点。
* **低分期 (Healthy/T1)**：解剖结构相似，强制特征重合（欧氏距离）。
* **高分期 (Severe/T4)**：形态各异但语义方向一致（均为破坏性），强制方向一致（余弦距离）。

$$L_{SAA} = \lambda_{low} \|\mu_0^s - \mu_0^t\|_2^2 + \lambda_{high} \left( 1 - \frac{\mu_{max}^s \cdot \mu_{max}^t}{\|\mu_{max}^s\| \|\mu_{max}^t\|} \right)$$

> 其中 $\mu$ 代表当前 Batch 内计算出的某类别的特征中心（Prototype）。

#### 2. 共享疾病进展轴损失 ($L_{SPA}$)
**目的**：学习一个跨越器官限制的通用“病情恶化”映射。

$$z_i = \text{Sigmoid}(v^T f_i), \quad L_{SPA} = \frac{1}{N} \sum_{i=1}^N \|z_i - \tilde{y}_i\|^2$$

> $\tilde{y}_i = y_i / K$ 为归一化后的序数标签，消除了不同器官分期数 $K$ 不同的影响。

### 二、 序数回归与对比学习（原有核心）

#### 3. 改进的加权序数对比损失 ($L_{SCOLw}$)
$$L_{SCOLw}=\sum_{i \in B}-w_i\frac{1}{|P(i)|}\sum_{p \in P(i)}\log\frac{\exp(f_i^T \cdot f_p / \tau)}{\sum_{n \in N(i)}\exp((f_i^T \cdot f_n + r_{i,n}) / \tau)}$$

#### 4. 原型对比序数损失 ($L_{PCOL}$)
$$L_{PCOL}=-\sum_{i \in \mathcal{I}}\log\frac{\exp(f_a^T \cdot c_p / \tau)}{\sum_{c_n \in N(i)}\exp((f_a^T \cdot c_n + r_{a,n}) / \tau)}$$

### 三、 解剖特征与鲁棒性（辅助任务）

* **5. 边界穿透感知损失 ($L_{BTA}$)**：$L_{BTA}=1-\cos(f_i, P_{trans})$（仅在 $y \ge 3$ 时激活）。
* **6. 拓扑保持流形损失 ($L_{Topo}$)**：$L_{Topo}=\sum_{i,j}W_{ij}\|f_i - f_j\|^2$。
* **7. 连续回归预测损失 ($L_{RMSE}$)**：$L_{RMSE}=\sqrt{\frac{1}{N}\sum_{i=1}^N(y_i - \hat{y}_i)^2}$。
* **8. 一致性正则化损失 ($L_{Consist}$)**：$L_{Consist}=D_{KL}(\sigma(E(Aug_{weak}(x_u))) \| \sigma(E(Aug_{strong}(x_u))))$。

---

## 第三部分：融合 SAA/SPA 的三阶段训练流程

### 阶段一：骨干网热身与轴线建立 (Warm-up & Axis Initialization)
* **输入**：全量前列腺癌数据 ($D_{Src}$)。
* **损失**：$L_{Phase1} = L_{RMSE} + L_{SPA} + L_{SCOLw}$。
* **目的**：建立 SPA 坐标系，使网络理解“轻微”与“严重”的通用生物学含义。

### 阶段二：多任务联合优化与跨域对齐 (Joint Training & Alignment)
1. **跨域对齐计算**：提取特征，计算 $s$ 和 $t$ 的锚点，应用 $L_{SAA}$ 拉齐起止点，应用 $L_{SPA}$ 强制样本落入通用轴线。
2. **数据分流**：
    * **路径 A (前列腺)**：$L_{Original} + L_{SAA} + L_{SPA}$。
    * **路径 B (直肠干净)**：$L_{Original} + L_{SAA} + L_{SPA} + L_{BTA}$。
    * **路径 C (直肠模糊)**：$L_{Consist}$ (无监督)。
* **总损失**：$L_{Total} = L_{Task\_Heads} + \alpha L_{SAA} + \beta L_{SPA} + \lambda L_{Consist}$。

### 阶段三：精细化微调 (Fine-tuning)
* **操作**：锁定 Backbone 浅层与 SPA Head，仅微调 Regression Head 和 BTA Head。
* **损失**：$L_{Phase3} = L_{RMSE} + L_{BTA}$。
* **目的**：针对直肠癌特有的临床阈值进行精度收敛。

---

## 结论：核心优势
1. **坐标系统一 (By SPA)**：学习脱离器官限制的疾病演进本质。
2. **两端锚定 (By SAA)**：解决中间分期难以对齐的痛点。
3. **鲁棒分流 (By Co-teaching)**：确保对齐操作不受错误标签干扰。
