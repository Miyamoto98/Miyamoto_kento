# 跨器官肿瘤进展分析模型架构方案 (Full Version)

## 第一部分：模型架构深度解构

本架构采用 **“共享骨干网 +异构多头并行”** 的非对称设计。为了克服跨器官（从前列腺到直肠）之间由于组织解剖结构不同导致的特征分布差异（Domain Shift），我们引入了 **SPA 轴向投影头**。该设计利用强标注的源域（前列腺）提取通用的肿瘤浸润语义，并强制源域与目标域在潜空间实现端对端对齐。



### 1. 共享骨干网 (Shared Backbone, $E$)

* **模型选型**：
  `EfficientNet-V2S`

* **设计逻辑**：
  作为通用的“解剖特征编码器”。在生物学层面，无论是前列腺癌还是直肠癌，其底层的 MRI 视觉特征具有高度的一致性，例如：在 T2 加权成像下的低信号线断裂、边缘的毛刺状改变以及组织纹理异质性。共享骨干网旨在捕获这些跨器官的共性特征。

* **输出特征嵌入向量定义**：

$$
f \in \mathbb{R}^D
$$

### 2. 并行任务投影头 (Parallel Task-specific Heads)

在编码器输出特征之后，系统并行挂载了以下 **六个** 功能模块，以实现多任务联合学习：

* **[NEW] 共享进展轴头 (SPA Head)**：
  * **物理结构**：由一个共享权重向量 $v$ 和 Sigmoid 激活函数组成。
  * **核心作用**：将高维特征向量压缩为一维的 $[0,1]$ 标量。这个标量代表了“疾病严重度”的绝对刻度，用于在不同器官之间建立一个统一的、标准化的全局对齐参考系。

* **原型投影头 (PCOL Head)**：
  在潜空间内为每个分期建立全局的类别中心（Prototypes），通过对比学习增强类内紧凑性。

* **序数对比头 (SCOLw Head)**：
  在不同样本之间建立严谨的序数距离约束，确保模型理解“T1 < T2 < T3”的逻辑顺序。

* **边界感知头 (BTA Head)**：
  专门用于提取图像的高频特征（High-frequency features），其任务是精准判断肿瘤是否处于“穿透边界”的关键状态。

* **拓扑保持头 (Topo Head)**：
  通过图拉普拉斯正则化（Graph Laplacian Regularization）来维持疾病进展在特征空间中的连续流形结构。

* **回归预测头 (Regression Head)**：
  最终输出连续的预测值：

$$
\hat{y}
$$

这是推理阶段的核心输出，用于给出精细化的分期评分。

---

## 第二部分：全口径损失函数数学建模

本方案集成了 8 个精心设计的损失函数，旨在从“特征对齐”、“序数约束”和“解剖感知”三个维度对模型进行全方位的监督。

### 一、 跨域对齐与全局约束 (Domain Alignment)

#### 1. 跨器官软锚定对齐损失 ($L_{SAA}$)

**设计目的**：在特征空间中强行拉齐源域（前列腺 $s$）和目标域（直肠 $t$）的演进轨迹。

* **低分期阶段 (Healthy/T1)**：由于解剖结构尚未被破坏，表现高度相似，因此强制其特征中心点重合（采用欧氏距离）。
* **高分期阶段 (Severe/T4)**：虽然器官形态各异，但癌症的破坏性语义是一致的（均为浸润破坏），因此强制其特征向量在方向上保持一致（采用余弦距离）。

$$
L_{SAA} = \lambda_{low} \underbrace{\|\mu_0^s - \mu_0^t\|_2^2}_{\text{起点重合}} + \lambda_{high} \underbrace{\left( 1 - \frac{\mu_{max}^s \cdot \mu_{max}^t}{\|\mu_{max}^s\| \|\mu_{max}^t\|} \right)}_{\text{终点同向}}
$$

其中，下述符号代表当前 Batch 内计算出的特定类别（分期）的特征中心点（Prototype）：

$$
\mu
$$

其具体计算公式为：

$$
\mu = \frac{1}{|B_k|} \sum_{i \in B_k} f_i
$$

#### 2. 共享疾病进展轴损失 ($L_{SPA}$)

**设计目的**：学习一个能够跨越器官生理限制的通用“病情恶化”映射函数。预测值定义为：

$$
z_i = \text{Sigmoid}(v^T f_i)
$$

损失函数定义为：

$$
L_{SPA} = \frac{1}{N} \sum_{i=1}^N \|z_i - \tilde{y}_i\|^2
$$

其中，下述符号为归一化后的序数标签，目的是消除不同器官之间总分期数 $K$ 不一致的影响：

$$
\tilde{y}_i
$$

计算方式为：

$$
\tilde{y}_i = y_i / K
$$

### 二、 序数回归与对比学习 (Ordinal Learning)

#### 3. 改进的加权序数对比损失 ($L_{SCOLw}$)

**设计目的**：通过引入标签距离感知，有效解决医学影像数据中的长尾分布问题以及相邻分期之间的视觉歧义。

$$
L_{SCOLw}=\sum_{i \in B}-w_i\frac{1}{|P(i)|}\sum_{p \in P(i)}\log\frac{\exp(f_i^T \cdot f_p / \tau)}{\sum_{n \in N(i)}\exp((f_i^T \cdot f_n + r_{i,n}) / \tau)}
$$

其中，下述变量是针对标签之间序数距离的正则化项：

$$
r_{i,n}
$$

其约束逻辑为：

$$
r_{i,n} \propto |y_i - y_n|
$$

#### 4. 原型对比序数损失 ($L_{PCOL}$)

**设计目的**：确保单一样本特征向其所属的全局分期中心靠拢。

$$
L_{PCOL}=-\sum_{i \in \mathcal{I}}\log\frac{\exp(f_a^T \cdot c_p / \tau)}{\sum_{c_n \in N(i)}\exp((f_a^T \cdot c_n + r_{a,n}) / \tau)}
$$

### 三、 解剖特征与鲁棒性 (Anatomy & Robustness)

#### 5. 边界穿透感知损失 ($L_{BTA}$)

**设计目的**：针对 T2（未穿透）与 T3（已穿透）这一决定性的临床分界点进行强化。该损失函数仅在样本标签满足以下条件时激活：

$$
y \ge 3
$$

$$
L_{BTA}=1-\cos(f_i, P_{trans})
$$

#### 6. 拓扑保持流形损失 ($L_{Topo}$)

**设计目的**：利用图论方法保持特征空间中疾病演进的拓扑连续性。

$$
L_{Topo}=\sum_{i,j}W_{ij}\|f_i - f_j\|^2
$$

#### 7. 连续回归预测损失 ($L_{RMSE}$)

**设计目的**：直接优化预测分期数值与真实标签之间的均方根误差。

$$
L_{RMSE}=\sqrt{\frac{1}{N}\sum_{i=1}^N(y_i - \hat{y}_i)^2}
$$

#### 8. 一致性正则化损失 ($L_{Consist}$)

**设计目的**：专门针对模糊样本，通过强弱增强的一致性约束进行半监督学习。

$$
L_{Consist}=D_{KL}(\sigma(E(Aug_{weak}(x_u))) \| \sigma(E(Aug_{strong}(x_u))))
$$

---

## 第三部分：融合 SAA/SPA 的三阶段训练流程

### 阶段一：骨干网热身与轴线建立 (Warm-up & Axis Initialization)

* **执行逻辑**：利用前列腺数据量大、金标准标签准的优势，确立初始物理方向。

* **阶段一损失计算**：

$$
L_{Phase1} = L_{RMSE} + L_{SPA} + L_{SCOLw}
$$

### 阶段二：多任务联合优化与跨域对齐 (Joint Training & Alignment)

这是最核心的融合阶段。

1. **执行逻辑**：应用 $L_{SAA}$ 强制拉近两器官的起止点；应用 $L_{SPA}$ 强制所有样本落在同一条标准轴线上。

2. **多路径数据分流**：

* **路数 A (前列腺辅助路径)**：

$$
Loss = L_{Original} + L_{SAA} + L_{SPA}
$$

* **路数 B (干净直肠主路径)**：

$$
Loss = L_{Original} + L_{SAA} + L_{SPA} + L_{BTA}
$$

* **路数 C (模糊直肠抗噪路径)**：

$$
Loss = L_{Consist}
$$

* **本阶段总优化目标**：

$$
L_{Total} = L_{Task\_Heads} + \alpha L_{SAA} + \beta L_{SPA} + \lambda L_{Consist}
$$

### 阶段三：精细化微调 (Fine-tuning)

* **执行操作**：锁定前序参数，仅对回归预测头和边界感知头进行微调。

* **阶段三损失计算**：

$$
L_{Phase3} = L_{RMSE} + L_{BTA}
$$

---

## 结论：本方案的核心竞争优势

1. **坐标系统一 (By SPA Head)**：学习脱离器官生理限制的、通用的“肿瘤演进本质”。

2. **两端锚定策略 (By SAA Loss)**：利用“正常组织相似”和“晚期破坏一致”的生物学共性实现了稳健对齐。

3. **抗噪性与鲁棒性 (By Co-teaching)**：确保对齐操作在干净样本间进行，防止特征空间坍缩。
